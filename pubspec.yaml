name: urban_lens
description: "A new Flutter project."


version: 1.0.0+1

environment:
  sdk: ^3.9.2


dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

  # State Management
  flutter_riverpod: ^2.4.9
  # Functional Data & Value Equality (Crucial for State stability)
  freezed_annotation: ^2.4.1
  equatable: ^2.0.5
  # JSON Serialization
  json_annotation: ^4.8.1
  # Navigation (We will use GoRouter later, but let's prep)
  go_router: ^13.0.0
  # Functional programming helper (Optional but recommended for error handling)
  fpdart: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.4.8
  freezed: ^2.4.6
  json_serializable: ^6.7.1

  flutter_lints: ^5.0.0
flutter:

  uses-material-design: true


